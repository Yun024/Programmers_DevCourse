# tuple function
RED = (255, 0, 0)
custom_color = [255, 0, 0]

## 전용함수(.을 통해서 접근하는 함수) :method 

## 기본함수 : Built in Function
## 비파괴적인 함수는 사용가능
print(len(RED))
print(sorted(RED))
print(sorted(RED,reverse=True))

## RED.
## custom_color.
## tuple .index .count 같은 비파괴적인 함수 
## list 비파괴적/파괴적 함수 
## 자료구조와 컨트롤을 통해 설계도 확인 가능 