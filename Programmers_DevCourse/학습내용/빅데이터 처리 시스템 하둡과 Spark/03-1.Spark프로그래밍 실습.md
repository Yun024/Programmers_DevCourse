# Spark DataFrame 실습
### spark.catalog.listTables()
- Spark는 in-memory 카탈로그를 사용하는 것이 default
- 스토리지 기반의 카탈로그를 쓰고 싶다면 SparkSession 설정할 때 enableHiveSupport()를 호출
- Hive metastore를 카탈로그로 사용하여 Hive UDF와 Hive 파일포맷 사용 가능

### pyspark.sql.types
- [참고문서](https://spark.apache.org/docs/latest/sql-ref-datatypes.html)

### DataFrame의 컬럼을 지칭하는 방식
```python
from pyspark.sql.functions import col, column

# 아래 4개는 동일한 표현
stationTemps = minTemps.select(
    "stationID",
    col("stationID"),
    column("stationID"),
    minTemps.stationID
)
```

## namegender실습
- pyspark를 실행해서 python 접속
```python
import os
os.getcwd() # 해당 경로에 namegender.csv 위치
df = spark.read.option("header",True).csv("name_gender.csv")
df.show(5)
df.groupby(["gender"]).count().collect()
exit()
```

## pi.py실습
```bash
- python을 실행하지 않고 bash에서 spark-submit 
- 경로 확인: pi.py 경로 확인해서 입력
spark-submit --master 'local[4]' ./spark-3.5.3-bin-hadoop3/examples/src/main/python/pi.py 
```

## 헤더가 없는 CSV 파일 처리하기1
- 입력 데이터: 헤더 없는 CSV 파일 - '1800.csv'
- 데이터에 스키마 지정하기
- SparkConf 사용해보기
- measure_type값이 TMIN인 레코드 대상으로 stationId별 최소 온도 찾기 
    + 판다스와 비교

## 헤더가 없는 CSV 파일 처리하기2
- 입력 데이터: 헤더 없는 CSV파일 - 'customer-oreders.csv'
- 데이터에 스키마 지정하기
    + cust_id, item_id, amount_spent를 데이터 컬럼으로 추가하기(모두 숫자)
- cust_id를 기준으로 amount_spent의 합을 계산하기

## 텍스트를 파싱해서 구조화된 데이터로 변환하기
- Regex를 이용해서 아래와 같이 변환해보는 것이 목표
- 입력: "On 2021-01-04 the cost per ton from 85001 to 85002 is $28.32 at ABC Hauling"
    + regex 패턴: "On (\S+) the cost per ton from (\d+) to (\d+) is (\S+) at (.*)"
        - \S (non-whitespace character), \d (numeric character)

## Stackoverflow 서베이 기반 인기 언어 찾기
- stackoverflow CSV파일에서 아래 두 필드는 ;를 구분자로 프로그래밍 언어를 구분
    + LanguageHaveWorkedWith
    + LanguageWantToWorkWith
- 이를 별개 레코드로 분리하여 아래 주제를 계산
    + 가장 많이 사용되는 언어 top 50
    + 가장 많이 쓰고 싶은 언어 top50

## Snowflake 연결해보기
- MAU(Monthly Active User)계산해보기
- 두 개의 테이블을 Snowflake에서 Spark으로 로드
    + JDBC 연결 실습
- DataFrame과 SparkSQL을 사용해서 조인
- DataFrame JOIN
    + left_DF.join(right_DF, join condition, join type)
        - join type: "inner", "left", "right", "outer", "semi", "anti"
